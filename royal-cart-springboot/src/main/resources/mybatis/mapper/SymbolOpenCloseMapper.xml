<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.royal.mapper.SymbolOpenCloseMapper">

	<select id="getLastCreateTime" resultType="String">
		SELECT
			MAX(create_time)
		FROM
			b_symbol_open_close
	</select>

</mapper>